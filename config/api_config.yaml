online_sales_api:
  base_url: ${API_BASE_URL}
  endpoint: /online-sales
  health_endpoint: /health
  api_key: ${API_KEY}
  page_size: 500
  timeout_seconds: 30
  max_retries: 3
  backoff_seconds: 5
  max_pages: 200          # safety stop so a broken API cannot loop forever
